name: Create PR

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  create-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Create Pull Request with gh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          
          git remote add upstream https://github.com/Orsoniks/scavgame-locale.git
          git fetch upstream main
          
          if git diff --quiet upstream/main main -- "*.json"; then
            echo "No changes. Exiting."
            exit 0
          fi

          BRANCH_NAME="crowdin-sync-$(date +%s)"
          git checkout -b $BRANCH_NAME upstream/main
          
          git checkout main -- "*.json"
          
          git restore --staged EN.json PL.json || true
          git checkout upstream/main -- EN.json PL.json || true
          
          if git diff --cached --quiet; then
            echo "No changes. Exiting."
            exit 0
          fi

          git commit -m "Update translations from Crowdin"
          git push -u origin $BRANCH_NAME

          MODIFIED_FILES=$(git diff --name-status upstream/main $BRANCH_NAME | grep "\.json$" || true)
          STATS=$(git diff --shortstat upstream/main $BRANCH_NAME)
          
          PR_BODY="Automated Pull Request generated by scavgame-crowdin.

          **Changes Included:**
          $STATS

          **Modified Files:**
          \`\`\`
          $MODIFIED_FILES
          \`\`\`
          "

          gh pr create \
            --repo Orsoniks/scavgame-locale \
            --base main \
            --head ExpieIndex:$BRANCH_NAME \
            --title "Update translations from Crowdin" \
            --body "$PR_BODY"
